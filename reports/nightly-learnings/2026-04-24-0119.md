---
title: "Nightly Learnings 2026-04-24 -- 2026-04-24"
type: report
report_type: nightly-learnings
date: 2026-04-24
time: "01:19"
---

# Nightly Learnings 2026-04-24 -- 2026-04-24 01:19

# Nightly Learnings 2026-04-24

Sessions scanned: 331 Hermes session files in the last 24 hours.
Cron outputs checked: 0 files in `~/.hermes/cron/output` for this window.
Claude Code notes checked: 0 recent `.md` files under `~/.claude/projects`.
Candidates reviewed: 10 high-signal candidates.
Learnings promoted: 6.

New durable insights:
- Resolver validation should use `checkResolvable()` as the real gate; duplicate trigger-to-skill routes are often legitimate. Landing spot: `digests/nightly-learnings-patterns`.
- Explicit URLs should route to native integrations first, and those rules should stay config-driven. Landing spot: `digests/nightly-learnings-patterns`.
- Routing misses and candidate-skill promotion should share one evidence loop: telemetry first, nightly drift checks second, promotion last. Landing spot: `digests/nightly-learnings-patterns`.
- OpenAI RSS often needs a browser-like `User-Agent`, and `lastBuildDate` is the cheapest freshness probe. Landing spot: `digests/nightly-learnings-patterns`.
- Circleback sync reliability depends on 3-ID batching, small pauses between rapid timeline writes, and `gbrain` slug resolution instead of homemade slugify. Landing spot: `digests/nightly-learnings-patterns`.
- `embed --stale` pool-exhaustion failures are transient; later retries can still clear a real stale backlog. Landing spot: `digests/nightly-learnings-patterns`.

Deduped / skipped:
- The nightly-learnings local-skill + `gbrain` fallback was already captured in `skills/nightly-learnings-collector/skill`.
- Page-visible attachments vs `upload-raw` was already captured in `digests/nightly-learnings-patterns` on 2026-04-23.
- Circleback replacing Granola as the canonical meeting source was already captured in `digests/nightly-learnings-patterns` on 2026-04-23.
- Evidence-over-self-editing as the main Hermes improvement rule was already captured in `digests/nightly-learnings-patterns` on 2026-04-23.

Skills improved during the day:
- `nightly-learnings-collector`
- `rbrain-circleback-sync`
- `daily-ai-news-digest`
- `rbrain-save-document-page` (new)

Recurring theme:
- Hermes is moving from heuristic routing and ad hoc recovery toward measured promotion loops, config-driven tool selection, and explicit operational guardrails.
